<%= form_with(model: @memory) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Log new memory..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>